package com.learner.movieRecommendation.simulator;

import com.learner.movieRecommendation.service.MovieRecommender;

public interface MovieRecommendationSimulator {
    void simulate(MovieRecommender recommender);
}
